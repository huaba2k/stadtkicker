/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {}, // Hier steht jetzt wieder das normale 'tailwindcss'
    autoprefixer: {},
  },
};

export default config;