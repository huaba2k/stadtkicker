Options -MultiViews
RewriteEngine On

# 1. Entferne .html Endung in der URL (optional, sieht schöner aus)
RewriteCond %{THE_REQUEST} \s/+(.+?)\.html[\s?] [NC]
RewriteRule ^ /%1 [R=301,L,NE]

# 2. Leite Anfragen ohne Endung intern auf die .html Datei um
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^(.+)$ $1.html [L]

# 3. Falls gar nichts gefunden wird -> 404 Seite (Optional, Next.js macht meist eine 404.html)
ErrorDocument 404 /404.html